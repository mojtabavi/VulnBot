# Database Configuration (db_config.yaml)
# MySQL stores sessions, plans, tasks, conversations, messages.
mysql:
  host: 127.0.0.1
  port: 3306
  user: vulnbot
  password: change_me
  database: vulnbot
